let
  val a = 1
  val b = "foo"
in
  (a, b, 20)
end;
let
  val a = 1
  val b = "foo"
in
  a;
  (b, it)
end;
let
  val a = 1
  val b = "foo"
in
  (a, b, 20)
end : bigint * string * bigint;
let
  val a = 1
  val b = "foo"
in
  (a, b, 20)
end : bigint * string;
(let val 'a id : 'a -> 'a = fn z => z in id id end,
 let val f = fn (y: 'a) => y in f end);
